<resources>

    <!-- Base application theme. -->
    <style name="AppTheme_" parent="android:Theme.Holo.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="android:background">@color/Transparent_1</item>
        <item name="android:windowBackground">@drawable/fragment_background</item>
        <item name="android:colorBackground">@color/Transparent_1</item>
        <item name="android:button">@drawable/button</item>
        <item name="android:editTextBackground">@drawable/rounded_edittext</item>
    </style>

    <style name="Widget.Spinner" parent="android:Widget">
        <item name="android:background">@drawable/btn_dropdown</item>
        <item name="android:clickable">true</item>
    </style>


    <style name="Transparent">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Translucent</item>
        <item name ="android:windowBackground">@color/Transparent_1</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:colorForeground">#fff</item>
    </style>

</resources>
